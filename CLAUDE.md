# Claude Project Entry

Read first:

1. `.agent-project.yml` in this folder.
2. `../../../PROJECT_CONTEXT.md`
3. `../../../000_Agent/AGENT_ROUTING.md`
4. `../../../000_Agent/memory/MEMORY.md`
5. The memory files listed in `.agent-project.yml`.

Rules:

- Work from this folder for Logbook implementation tasks.
- Confirm the Git root before commit or push.
- Keep shared memory updates in `../../../000_Agent/memory/`; use `/recap` after meaningful decisions.
- Delegate large read-only scans or repetitive work to `agy`, but verify its output before applying changes.
- Do not write secrets here.
